---
name: "scope-gaps"
description: "Read the research brief + index.html, identify the best PR-sized gap, dedup, and file exactly one GitHub issue"
---

# scope-gaps

Run the **scope-gaps** stage. Follow every step in
[`skills/scope-gaps.md`](../../../skills/scope-gaps.md), treating the user's argument as the
capability to scope. That shared body is the single source of truth; this file only wires it into
the Copilot CLI. See `pipeline.json` stage `scope`.
