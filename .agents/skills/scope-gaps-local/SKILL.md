---
name: "scope-gaps-local"
description: "Read brief + index.html, identify one PR-sized gap, dedup, and file exactly one local-routed issue"
---

# scope-gaps-local

Run the local scoping stage for capability work. Follow every step in
[`skills/scope-gaps-local.md`](../../../skills/scope-gaps-local.md), treating the
user's argument as the capability. This files/dedups exactly one issue labeled
`agent-found-local` (not `agent-found`) for use with `/shiplocal`.
