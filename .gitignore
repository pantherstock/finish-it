# Private planning notes — never publish
plan/

# Test tooling (installed transiently in CI, never committed)
node_modules/
package.json
package-lock.json
test-results/
playwright-report/

# OS / editor noise
.DS_Store
Thumbs.db
*.log

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
